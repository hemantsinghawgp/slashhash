<!DOCTYPE html>
<html lang="en">
  <head>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container p-5">
        <div class="card ">


            <div class="card-header fw-bold lead"></i> Search for movies and TV shows 
                <span class="float-end">
                    <a href="/favorites" class="btn btn-dark btn-sm"> View my favorites </a>
                </span>
            </div>

            <div class="card-body ">
               
                    <div class="form">
                      <form action="/search" method="POST">
                        <input
                          type="text"
                          name="searchQuery"
                          class="form-control form-input"
                          placeholder="Enter a movie title"
                        />
                        <br />
                        <button type="submit" class="btn btn-primary">Search</button>
                      </form>
                    </div>
                 
            </div>
        </div>
     
    </div>
  </body>
</html>
